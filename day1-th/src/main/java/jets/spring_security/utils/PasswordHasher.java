package jets.spring_security.utils;

// Used if you will hash the password to save in the database.
public class PasswordHasher {

    public static String hash(String password) {
        return password;
    }
}
